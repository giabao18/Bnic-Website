import styled from "@emotion/styled";
import { Avatar } from "antd";
import { customColors } from "..";

export const CustomAvatarAboutPage = styled(Avatar)({
    backgroundColor: `${customColors.colorPrimary}`
})